package com.yuku.browser.core

/** Which order the New Tab sheet's "Recently visited" list uses. */
enum class HistorySort(val label: String) {
    MostRecent("Most recent"),
    MostVisited("Most visited"),
}
